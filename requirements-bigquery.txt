pandas==1.0.3
numpy==1.18.4
pyodbc==4.0.27
sqlalchemy==1.3.11
requests==2.22.0
BeautifulSoup4==4.8.1
haversine==2.1.2
jupyterlab==2.1.2
seaborn==0.10.1
rootpath==0.1.1
dbt==0.19.0

# pin pycryptodomex: newer versions require compiling on windows
# which is a pain, and it's used for snowflake, which we don't use anyway
pycryptodomex==3.9.9

# agate >= 1.6.2 requires PyICU which isn't installable via pip
# https://github.com/fishtown-analytics/dbt/pull/3161
# so pin to 1.6.1
agate==1.6.1

google-cloud-bigquery-storage==2.1.0
google-cloud-storage==1.32.0
# required for bigquery's db-api adapter to return results properly
pyarrow==0.17.1
